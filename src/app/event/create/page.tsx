import CustomizedSteppers from "@/sections/event-stepper/CustomizedStepper";

const page = () => {
    return (
        <div>
            <CustomizedSteppers />
        </div>
    );
}

export default page;