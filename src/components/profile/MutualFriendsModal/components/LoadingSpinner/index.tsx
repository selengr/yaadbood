// style
import {
    StyledDotPulse,
    StyledLoadingContainer
 } from './loadingSpinner.style';

const LoadingSpinner = () => (
    <StyledLoadingContainer>
      <StyledDotPulse />
    </StyledLoadingContainer>
);

export default LoadingSpinner